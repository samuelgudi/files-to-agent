services:
  files-to-agent:
    image: ghcr.io/samuelgudi/files-to-agent:latest
    pull_policy: always
    container_name: files-to-agent
    restart: unless-stopped
    env_file: .env
    networks:
      - agent-net
    volumes:
      - ./data:/data
    expose:
      - "8080"

networks:
  agent-net:
    name: agent-net
    external: true
